apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "derli.fullname" . }}-test-connection"
  labels:
    {{- include "derli.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "derli.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
